<!--suppress HtmlDeprecatedAttribute, HttpUrlsUsage -->

<div align="center">
  <p>
    <picture>
      <source srcset="https://github.com/SuperMonster003/AutoJs6-Plugin-Angus-Mail/blob/master/app/src/main/res/mipmap-night/ic_launcher.png?raw=true" media="(prefers-color-scheme: dark)" />
      <img src="https://github.com/SuperMonster003/AutoJs6-Plugin-Angus-Mail/blob/master/app/src/main/res/mipmap/ic_launcher.png?raw=true" alt="autojs6-plugin-angus-mail-ic-launcher" border="0" width="128" />
    </picture>
  </p>

  <p>يرسل البريد ويستقبله ويبحث فيه ويراقبه من نصوص AutoJs6 البرمجية عبر IMAP و POP3 و SMTP</p>

  <p>
    <a href="https://github.com/SuperMonster003/AutoJs6-Plugin-Angus-Mail/releases"><img alt="GitHub release (latest by date)" src="https://img.shields.io/github/v/release/SuperMonster003/AutoJs6-Plugin-Angus-Mail?label=Release"/></a>
    <a href="https://github.com/SuperMonster003/AutoJs6-Plugin-Angus-Mail/issues"><img alt="GitHub closed issues" src="https://img.shields.io/github/issues/SuperMonster003/AutoJs6-Plugin-Angus-Mail?color=A24232&label=Issues"/></a>
    <a href="https://github.com/SuperMonster003/AutoJs6-Plugin-Angus-Mail/blob/master/LICENSE"><img alt="GitHub License" src="https://img.shields.io/github/license/SuperMonster003/AutoJs6-Plugin-Angus-Mail?color=534BAE&label=License"/></a>
  </p>
</div>

******

### اللغات

******

يدعم README.md الحالي اللغات التالية:

- [简体中文 [zh-Hans]](https://github.com/SuperMonster003/AutoJs6-Plugin-Angus-Mail/blob/master/.readme/README-zh-Hans.md)
- [繁體中文 (香港) [zh-Hant-HK]](https://github.com/SuperMonster003/AutoJs6-Plugin-Angus-Mail/blob/master/.readme/README-zh-Hant-HK.md)
- [繁體中文 (台灣) [zh-Hant-TW]](https://github.com/SuperMonster003/AutoJs6-Plugin-Angus-Mail/blob/master/.readme/README-zh-Hant-TW.md)
- [English [en]](https://github.com/SuperMonster003/AutoJs6-Plugin-Angus-Mail/blob/master/.readme/README-en.md)
- [Français [fr]](https://github.com/SuperMonster003/AutoJs6-Plugin-Angus-Mail/blob/master/.readme/README-fr.md)
- [Español [es]](https://github.com/SuperMonster003/AutoJs6-Plugin-Angus-Mail/blob/master/.readme/README-es.md)
- [日本語 [ja]](https://github.com/SuperMonster003/AutoJs6-Plugin-Angus-Mail/blob/master/.readme/README-ja.md)
- [한국어 [ko]](https://github.com/SuperMonster003/AutoJs6-Plugin-Angus-Mail/blob/master/.readme/README-ko.md)
- [Русский [ru]](https://github.com/SuperMonster003/AutoJs6-Plugin-Angus-Mail/blob/master/.readme/README-ru.md)
- العربية [ar] # الحالي

******

### مقدمة

******

يوفر Angus Mail لنصوص AutoJs6 البرمجية كائنا عاما باسم `mail` لإرسال الرسائل, وسرد صناديق البريد والبحث فيها, وقراءة نصوص الرسائل, وتنزيل المرفقات, وإدارة العلامات والمجلدات, ومراقبة وصول بريد جديد إلى مجلد. يعتمد على [Eclipse Angus Mail](https://eclipse-ee4j.github.io/angus-mail/) 2.0.5, التنفيذ المرجعي لـ Jakarta Mail, ويتعامل مع IMAP و POP3 و SMTP عبر TLS.

تبقى كل حركة البريد داخل عملية المكون الإضافي. يكتشف AutoJs6 المكون الإضافي عبر خدمة Binder الخاصة به, ويسلمه الحساب الذي يقدمه النص البرمجي (أو اسما مستعارا محفوظا في صفحة إعدادات المكون الإضافي), ويتلقى نتائج JSON وتدفقات المرفقات; ولا يحتوي المضيف نفسه على أي شيفرة بريد. تبقى بيانات الاعتماد في الذاكرة طوال مدة الجلسة فقط ما لم تختر حفظ حساب في المكون الإضافي.

******

### الحالة

******

يضيف الإصدار 1.2.1 تسجيل الدخول عبر المتصفح لحسابات Google و Microsoft (خارطة الطريق P9) فوق المراقبة في الخلفية من 1.1.0 (خارطة الطريق P8); صدرت كل بنود المراحل P0 إلى P8 مع 1.0.0 إلى 1.1.0, والأدلة في [ROADMAP.md](https://github.com/SuperMonster003/AutoJs6-Plugin-Angus-Mail/blob/master/ROADMAP.md). يتطلب AutoJs6 6.8.0 (البناء 5282) أو أحدث; تحتاج مهمة "عند وصول بريد" إلى بناء المضيف الذي يحمل عقد البريد الإصدار 2; المرجع الكامل لواجهة برمجة السكربتات في [وثائق AutoJs6](https://docs.autojs6.com/#/mail).

******

### الميزات

******

توفر الإضافة القدرات التالية:

- الإرسال: نص عادي أو HTML, وعدة مستلمين, ومرفقات وصور مضمنة, وترويسات مخصصة وأولوية, مع حفظ نسخة المرسل على الخادم عندما لا يقوم المزود بذلك بنفسه.
- الاستقبال: سرد مجلد صفحة بصفحة, والبحث على الخادم (مع الرجوع إلى ترشيح على جانب العميل لدى المزودين الذين يرفضون البحث بغير ASCII), وقراءة النصوص بصيغتي النص و HTML, وتنزيل المرفقات مباشرة إلى دليل عمل النص البرمجي.
- التنظيم: وضع علامة مقروء أو مميز, والنقل, والنسخ, والحذف, والحذف النهائي, وإنشاء المجلدات وإعادة تسميتها وحذفها; تحصل حسابات POP3 على المجموعة الفرعية للقراءة فقط.
- المراقبة: تلقي أحداث البريد الجديد طوال مدة تشغيل النص البرمجي, عبر IMAP IDLE حيث يدفع الخادم فعلا وعبر الاستطلاع الدوري (60 ثانية افتراضيا, قابل للضبط) حيث لا يفعل: QQ و Sina يقبلان IDLE لكنهما لا يرسلان شيئا, و 163 و 126 بلا IDLE, وحسابات POP3 تستطلع دائما; تنجو المراقبة من انقطاع الشبكة ومن إعادة تشغيل عملية الإضافة.
- المراقبة في الخلفية: تبقي صفحة المراقبات في الإعدادات اتصالات IMAP IDLE أو الاستطلاع بالحسابات المحفوظة في خدمة مقدمة بينما لا يعمل أي سكربت, وتسجل كل رسالة جديدة, وتوقظ مهمة AutoJs6 "عند وصول بريد" للمراقبة المختارة مع تصفية اختيارية بالمرسل والموضوع; تصل الرسالة إلى السكربت في `engines.myEngine().execArgv.mail`.
- المزودون: إعدادات مسبقة لـ Gmail و Outlook.com و Microsoft 365 و QQ و 163 و 126 و iCloud و Yahoo و Sina و Aliyun تملأ المضيفين والمنافذ والتشفير; ويمكن تجاوز أي حقل لخوادم أخرى.
- المصادقة: كلمات المرور ورموز التفويض من المزودين, أو رموز وصول XOAUTH2 يقدمها النص البرمجي مع دالة تجديد.
- تسجيل الدخول عبر المتصفح: يمكن إضافة حساب Gmail أو Outlook.com أو Microsoft 365 بتسجيل الدخول بحساب Google أو Microsoft في متصفح النظام من إعدادات الإضافة (رمز تفويض OAuth 2.0 مع PKCE); تحتفظ الإضافة برمز التحديث مشفرا على الجهاز, وتجدد رمز الوصول قبل كل جلسة, وتعرض حالة تسجيل الدخول مع "تسجيل الدخول مجددا" و "إلغاء تسجيل الدخول" في صفحة الحسابات. تستمر السكربتات بالاتصال بالاسم المستعار ولا ترى أي رمز.

******

### الاستخدام

******

1. ثبت ملف APK للمكون الإضافي من [Releases](https://github.com/SuperMonster003/AutoJs6-Plugin-Angus-Mail/releases) على جهاز مثبت عليه AutoJs6 بالبنية 5282 (6.8.0) أو أحدث.
2. افتح مركز المكونات الإضافية في AutoJs6, وتأكد من التعرف على `Angus Mail` ثم فعله.
3. جهز الحساب: فعل IMAP أو POP3 و SMTP في إعدادات الويب لدى المزود واحصل على رمز تفويض (QQ, 163, 126, Sina) أو كلمة مرور تطبيق (Gmail, iCloud, Yahoo); كلمة مرور تسجيل الدخول نفسها لا تقبل عادة. يمكن بدلا من ذلك تسجيل الدخول إلى حسابات Gmail و Outlook.com و Microsoft 365 بحساب Google أو Microsoft في المتصفح من إعدادات الإضافة (اختر المصادقة "تسجيل الدخول بحساب Google / Microsoft (المتصفح)"), أو إعطاؤها رمز وصول OAuth 2.0 تم الحصول عليه في مكان آخر.
4. استدع `mail.connect(...)` في نص برمجي, أو احفظ الحساب في صفحة إعدادات المكون الإضافي (أيقونته في المشغل, أو AutoJs6 > خيارات المطور > إعدادات حسابات البريد) واتصل بالاسم المستعار.
5. لتشغيل سكربت عند وصول بريد دون إبقاء سكربت يعمل: أضف مراقبة في صفحة المراقبات في الإضافة (الإعدادات > المراقبات: الاسم المستعار للحساب, المجلد, الوضع, المرشحات), واسمح بالإشعار عند الطلب, ثم أنشئ مهمة في AutoJs6 (اضغط مطولا على السكربت > مهمة مجدولة > التشغيل عند بث > عند وصول بريد) واختر المراقبة; تحتاج المهمة إلى بنية AutoJs6 بإصدار عقد البريد 2.

******

### تجهيز مزودي الخدمة

******

يحتاج كل مزود أولا إلى تفعيل IMAP (أو POP3) و SMTP في إعدادات الويب, وإلى رمز تفويض أو كلمة مرور تطبيق أو رمز وصول بدلا من كلمة مرور تسجيل الدخول; أهم ما يخص كل إعداد مسبق (قيمة `provider`):

- بريد QQ (`qq`): فعل خدمة IMAP/SMTP في إعدادات الحساب على الويب وأنشئ رمز تفويض يستخدم بوصفه `password`.
- 163 / 126 / yeah.net (`163` و `126`; يستخدم yeah.net الإعداد المسبق `163` مع استبدال المضيفين): فعل الخدمات في صفحة POP3/SMTP/IMAP من إعدادات الويب وأنشئ رمز تفويض; يفعل POP3 على حدة, وإلا رفض POP3 الرمز الذي يقبله IMAP و SMTP. تشترط الخوادم أن يرسل كل اتصال IMAP الأمر `ID` أولا (وإلا أجابت `Unsafe Login`); ويتولى المكون الإضافي ذلك بنفسه.
- بريد Sina (`sina`): فعل خدمة IMAP/SMTP في إعدادات العميل على الويب واستخدم رمز التفويض. لا يحتفظ الخادم بنسخة من البريد المرسل (يضيف المكون الإضافي نسخة إلى مجلد المرسل), ويرفض إنشاء المجلدات عبر IMAP, وتجري عمليات البحث النصي على جهة العميل.
- أبسط طريق هو تسجيل الدخول عبر المتصفح في صفحة إعدادات الإضافة ("تسجيل الدخول بحساب Google (المتصفح)"), الذي لا يحتاج إلى كلمة مرور تطبيق ويجدد رمزه بنفسه. وإلا: Gmail (`gmail`): بعد تفعيل التحقق بخطوتين أنشئ كلمة مرور تطبيق في حساب Google واستخدمها بوصفها `password`, أو قدم رمز وصول OAuth 2.0 بالنطاق `https://mail.google.com/` (`accessToken` مع `tokenProvider`); توجد المجلدات ضمن مساحة الأسماء `[Gmail]` ويصل البريد الجديد عبر IDLE. تحقق المشروع من الحساب الحقيقي برمز وصول.
- أبسط طريق هو تسجيل الدخول عبر المتصفح في صفحة إعدادات الإضافة ("تسجيل الدخول بحساب Microsoft (المتصفح)"), الذي يحصل على الرمز ويجدده بنفسه. وإلا: Outlook.com / Hotmail (`outlook`) و Microsoft 365 (`office365`): عطلت Microsoft المصادقة الأساسية للحسابات الشخصية, لذا ترفض كلمات مرور التطبيقات على IMAP و POP3 و SMTP, ولا يقبل الإعداد المسبق `outlook` سوى رمز وصول OAuth 2.0 (`accessToken` مع `tokenProvider`); تقبل حسابات العمل أو المدرسة (`office365`) كلمة مرور أو رمز وصول, لكن سياسة المستأجر قد تعطل IMAP أو POP3 أو SMTP AUTH. يحتاج الرمز إلى النطاقات المفوضة `IMAP.AccessAsUser.All` و `POP.AccessAsUser.All` و `SMTP.Send` من `https://outlook.office.com/` (تحقق المشروع من حساب شخصي بمثل هذا الرمز: IMAP و POP3 و SMTP ودفع IDLE); في بعض صناديق البريد الشخصية الأحدث عطلت Microsoft مصادقة SMTP AUTH (`535 5.7.139`) ولا يوجد إعداد للمستخدم يفعلها.
- iCloud (`icloud`): أنشئ كلمة مرور خاصة بالتطبيق في حساب Apple; لا توجد خدمة POP3.
- Yahoo (`yahoo`) وبريد Aliyun الشخصي (`aliyun`): أنشئ كلمة مرور تطبيق أو رمز تفويض; يتبع هذان الإعدادان المسبقان التوثيق العام ولم يتم التحقق منهما لأن المشروع لا يملك حساب اختبار.

للخوادم الأخرى يترك `provider` فارغا وتحدد `host` و `port` و `tls` (`ssl` أو `starttls` أو `none`) لكل من `imap` (أو `pop3`) و `smtp`; ويمكن أيضا استبدال أي حقل من حقول الإعداد المسبق. جميع الخيارات موصوفة في [MailAccountOptions](https://docs.autojs6.com/#/mailAccountOptionsType), ويعرض `mail.providers.list()` الإعدادات المسبقة المدمجة.

******

### بداية سريعة

******

نص برمجي يتصل بالاسم المستعار, ويرسل تقريرا, ويقرأ البريد غير المقروء مع مرفقاته, ويراقب رمز تحقق, ويبحث بشكل غير متزامن:

```js
// A saved alias keeps the credential inside the plugin; an inline account works as well:
// mail.connect({ provider: 'qq', address: 'me@qq.com', password: 'authorization-code' })
let client = mail.connect('work');

client.send({ to: 'you@example.com', subject: 'Report', text: 'See the attachment', attachments: ['/sdcard/report.xlsx'] });

client.fetch({ unseenOnly: true, limit: 10 }).forEach(m => {
    let full = m.load();
    full.attachments.forEach(a => a.download(files.join(files.cwd(), 'mail-attachments')));
    client.markRead(m);
});

let watch = client.watch('INBOX', { fetchBody: true });
watch.on('message', m => { if (/code/i.test(m.subject)) console.log(m.text); });
watch.on('error', e => console.warn(e.code, e.message));

// Every network method also has an Async form; every failure is a MailError with a code.
mail.setDefault(client);
mail.searchAsync({ subject: 'invoice', since: '2026-09-01' }).then(list => console.log(list.length, list.fallback));
```

******

### الحسابات المحفوظة والأسماء المستعارة

******

الاسم المستعار هو حساب محفوظ داخل المكون الإضافي. بعد إدخال الحساب واختباره وحفظه في صفحة إعدادات المكون الإضافي (أيقونته في المشغل, أو AutoJs6 > خيارات المطور > إعدادات حسابات البريد), تخزن كلمة المرور أو رمز الوصول مشفرة بمفتاح Android Keystore في الدليل الخاص بالمكون الإضافي وتستثنى من النسخ الاحتياطي; ثم تتصل النصوص البرمجية عبر `mail.connect('alias')`, ولا تمر بيانات الاعتماد عبر النص البرمجي ولا عبر Binder.

يمكن لصفحة الإعدادات تعليم حساب واحد بوصفه الافتراضي; تحمل العناصر التي يعيدها `mail.accounts.list()` القيمة `default: true` لهذا الحساب, ويتحقق `mail.accounts.has(alias)` من وجود الاسم المستعار. أنشئ عميلا لكل اسم مستعار عند استخدام عدة حسابات معا; وبعد `mail.setDefault(client)` تعمل الأساليب المحولة مثل `mail.fetch(...)` على العميل الافتراضي.

******

### التوافق

******

الاستنتاجات التالية مستمدة من مصفوفة الحسابات الحقيقية للمرحلة P6 (2026-09-19 و 09-20: أرسل كل مزود رسالة إلى نفسه بموضوع ونص وأسماء عرض واسم ملف بالصينية, ثم جرى التحقق من كل عملية), ومن مصفوفات TLS ومجموعات الأحرف ودورة الحياة والأداء:

- بريد QQ: الإرسال والإدراج والنصوص والمرفقات والعلامات والنقل (`MOVE`) و POP3 كلها ناجحة; يجيب الخادم عن البحث بالصينية بصفر نتائج بدلا من خطأ, لذا يلزم `fallback: 'always'`; يعيد الخادم كتابة Message-ID للبريد الصادر; يرفض إنشاء المجلدات; تعمل المراقبة بالاستطلاع الدوري (IDLE لا يدفع أبدا), ويصبح البريد الجديد مرئيا على الخادم بعد 15-40 ثانية من التسليم.
- 163 / 126 / yeah.net: كلها ناجحة; يحتفظ الخادم بنسخة المرسل; لا يوجد IDLE فتعمل المراقبة بالاستطلاع; يجيب 163 عن البحث النصي في البريد الحديث بصفر نتائج (126 و yeah.net يعملان); تعود المسافات في اسم عرض المرسل شرطات سفلية; يجب تفعيل POP3 على yeah.net على حدة في إعدادات الويب.
- بريد Sina: ناجح; لا يقبل الخادم سوى شروط ALL و SINCE والعلامات, فتتحول عمليات البحث النصي تلقائيا إلى التصفية على جهة العميل; لا يوجد IDLE; يرفض إنشاء المجلدات; يضيف المكون الإضافي نسخة المرسل.
- Gmail: كل الصفوف ناجحة برمز وصول OAuth 2.0, ويصل البريد الجديد عبر IDLE (نحو 30 ثانية, وهو إيقاع إشعارات Gmail نفسه); تنجح كل عمليات البحث على الخادم بما فيها الصينية (لا يفعل المكون الإضافي `UTF8=ACCEPT`); تخزن كلمات IMAP المفتاحية المخصصة (الوحيد بين الستة); لا يتضمن عرض POP3 البريد الذي أرسله الحساب إلى نفسه.
- Outlook.com / Hotmail: برمز وصول OAuth 2.0 تمر كل الصفوف على حساب شخصي واحد (2026-09-21): أدوار المجلدات من الأسماء المتعارف عليها, يحتفظ الخادم بنسخة المرسل ويعيد كتابة Message-ID, يعمل `MOVE` وإنشاء المجلدات, يسجل POP3 الدخول بصيغة السطرين من `AUTH XOAUTH2`, ويدفع IDLE خلال نحو 10 ثوان (الأسرع بين المزودين السبعة); البحث في الخادم عن عنوان صيني يصيب لكنه قد يستغرق دقائق; ما زالت كلمات مرور التطبيقات مرفوضة على IMAP و POP3 و SMTP (`AUTH_MECHANISM_UNSUPPORTED`), وفي بعض صناديق البريد الشخصية الأحدث عطلت Microsoft مصادقة SMTP AUTH (`535 5.7.139`); لا توجد حسابات اختبار لكل من iCloud و Yahoo و Aliyun وإعداداتها المسبقة غير متحقق منها.
- TLS ومجموعات الأحرف: اختبر SSL الضمني و STARTTLS والنص الصريح والشهادات الموقعة ذاتيا (مع `tls.trustAll` وبدونه) وعدم تطابق اسم المضيف وعدم تطابق وضع المنفذ واحدا واحدا على IMAP و POP3 و SMTP, وتقابل الإخفاقات رموزا مميزة مثل `TLS_FAILED` و `TIMEOUT`; وجرى التحقق من الموضوعات وأسماء العرض والنصوص وأسماء الملفات بترميزات GB18030 و GBK و GB2312 و Big5 و ISO-2022-JP و EUC-KR و UTF-8 في الحالات المعلنة وغير المعلنة والمعلنة خطأ.
- الأجهزة ودورة الحياة: محاكي Android 7.0 (API 24) وهاتفا Sony (Android 9) و Redmi (Android 13); تحرر الاتصالات والارتباطات والخيوط في ثماني طرق لإنهاء النص البرمجي (الخروج العادي, و `exit()`, و `engines.stopAll()`, والإيقاف القسري للمضيف أو المكون الإضافي, والترقية في المكان, وتعطيل المكون الإضافي وإلغاء تثبيته); مع إطفاء الشاشة (Doze) تفقد المراقبة اتصالها وتستعيده عند استيقاظ الجهاز, ويمكن لصفحة الإعدادات طلب الإعفاء من تحسين البطارية لمراقبة دون انقطاع.
- خط الأساس للأداء: الإدراج والبحث والتنزيل والإرسال وساعة من الانتظار في IDLE على صندوق وارد محلي من 10000 رسالة مع مرفق بحجم 50 MiB على JVM و Redmi و Sony مسجلة في `docs/dev/p6-performance-baseline.md`; مستندات الرسائل محدودة (للعناوين والترويسات وشجرة MIME والنصوص المضمنة حدود), فلا تستطيع المدخلات العدائية تفجير الجلسة.

******

### الأسئلة الشائعة

******

- **كيف أتحرى `AUTH_FAILED`?** تأكد من استخدام رمز التفويض أو كلمة مرور التطبيق لا كلمة مرور تسجيل الدخول, ومن تفعيل البروتوكول في إعدادات الويب (يفعل IMAP و POP3 كل على حدة); واستدع `client.test()` لترى نتيجة نقطة الاستقبال و SMTP ورمز الخطأ لكل منهما على حدة. في حسابات رموز الوصول يعني `AUTH_FAILED` عادة انتهاء صلاحية الرمز; ومع وجود `tokenProvider` يجدده المكون الإضافي ويعيد المحاولة مرة واحدة. تبين حقول الخطأ `code` و `details` و `retryable` ما إذا كانت إعادة المحاولة مجدية.
- **يجيب 163 / 126 بـ `Unsafe Login`?** ترفض خوادم IMAP لدى NetEase الاتصالات التي لم ترسل الأمر `ID`; يرسل المكون الإضافي `ID` مباشرة بعد تسجيل الدخول على كل اتصال IMAP, فلا ينبغي أن يحدث ذلك. وإن حدث مع ذلك, فأعد تفعيل خدمة IMAP في إعدادات الويب وأنشئ رمز تفويض جديدا.
- **البحث بالصينية لا يجد شيئا?** تتعامل الخوادم مع البحث بغير ASCII بطرق مختلفة: يرفضه Sina (فيتحول المكون الإضافي بنفسه إلى التصفية على جهة العميل), بينما يجيب QQ و 163 بصفر نتائج دون خطأ (لا ينطلق `fallback: 'client'` الافتراضي). استخدم `fallback: 'always'` على هذه الحسابات وضيق النطاق بـ `since` أو `limit`; تجلب تصفية النصوص على جهة العميل كل مرشح على حدة وقد تكون بطيئة في صندوق بريد كبير.
- **نجح `mail.connect` لكن أول `fetch` يفشل?** يفتح `connect` جلسة المكون الإضافي فقط ولا يتصل بخادم البريد; وأول أسلوب شبكي هو الذي يسجل الدخول (SMTP عند أول إرسال). استدع `client.test()` للتحقق من الحساب مسبقا.
- **تتوقف المراقبة بعد إطفاء الشاشة?** يجمد Doze في Android شبكة التطبيقات في الخلفية, فتفقد المراقبة اتصالها ويبلغ عن البريد الجديد بعد دقائق من استيقاظ الجهاز (يعيد المكون الإضافي الاتصال فور انتهاء Doze). لمراقبة دون انقطاع, استخدم زر الإرشاد في صفحة الإعدادات لطلب الإعفاء من تحسين البطارية; ولا تعيش المراقبة إلا أثناء تشغيل النص البرمجي وتغلق عند انتهائه.
- **كيف أوصل Outlook.com / Hotmail?** عطلت Microsoft المصادقة الأساسية للحسابات الشخصية, لذا لا يقبل الإعداد المسبق أي كلمة مرور. احفظ الحساب في صفحة إعدادات الإضافة واختر المصادقة "تسجيل الدخول بحساب Microsoft (المتصفح)": تفتح صفحة تسجيل دخول Microsoft في المتصفح, ولا يعود إلى الإضافة سوى رمز التفويض, وتجدد الإضافة رمز الوصول بنفسها; ثم تتصل السكربتات بالاسم المستعار. لا يزال بإمكان السكربت تمرير رمز وصول تم الحصول عليه في مكان آخر بوصفه `accessToken` وتحديثه عبر `tokenProvider`. إذا قالت صفحة الحسابات "يلزم تسجيل الدخول مجددا" فقد رفض التجديد (ألغي تسجيل الدخول أو انتهت صلاحيته): افتح قائمة الحساب واختر "تسجيل الدخول مجددا".
- **ماذا يستطيع حساب POP3?** لا يوجد سوى `INBOX` و `uid` هو سلسلة UIDL; يعمل الإدراج والقراءة والتنزيل والحذف والمراقبة بالاستطلاع, بينما تجيب العلامات والنقل والنسخ والإلحاق والمسح وإدارة المجلدات بـ `UNSUPPORTED_OPERATION`; ويجري البحث على جهة العميل بشروط الغلاف فقط.

******

### الصلاحيات والأمان

******

يلتزم المكون الإضافي بحدود صريحة:

- نقاط دخول Binder محمية بإذن التوقيع `org.autojs.permission.PLUGIN`, لذا لا يمكن الوصول إليها إلا من AutoJs6; ولا يصدر المكون الإضافي أي مكونات أخرى.
- يخدم إذن INTERNET اتصالات IMAP و POP3 و SMTP بالخوادم التي يسميها السكربت, وللحسابات التي سجلت الدخول عبر المتصفح طلبات HTTPS إلى نقاط الرموز لدى Google (`oauth2.googleapis.com`) و Microsoft (`login.microsoftonline.com`) فقط: استبدال رمز التفويض, وتجديد رمز وصول على وشك الانتهاء, وإلغاء رمز Google عند الطلب. لا تقوم الإضافة بأي طلب آخر ولا تجمع أي بيانات.
- تنتقل كلمات المرور والرموز من النص البرمجي إلى المكون الإضافي في حقول Binder مخصصة, ولا تظهر أبدا في السجلات أو مستندات JSON أو رسائل الخطأ أو تقارير الأعطال, ولا تبقى في الذاكرة إلا طوال مدة الجلسة. الحسابات المحفوظة في صفحة الإعدادات مشفرة بمفتاح Android Keystore ومستبعدة من النسخ الاحتياطي. تخزن رموز تسجيل الدخول عبر المتصفح بالطريقة نفسها: لا يغادر رمز التحديث الجهاز أبدا, ولا يسلم إلى جلسة البريد سوى رمز الوصول, ولا يستطيع AutoJs6 ولا السكربت قراءتها; "إلغاء تسجيل الدخول" يتخلص منها فورا.
- تستخدم الاتصالات TLS افتراضيا (SSL أو STARTTLS حسب متطلبات المزود); ويجب طلب الاتصالات غير المشفرة والشهادات الموقعة ذاتيا صراحة لكل حساب.
- إذن REQUEST_IGNORE_BATTERY_OPTIMIZATIONS يخدم زر الإرشاد في صفحة الإعدادات فقط: يعرض الزر ما إذا كان النظام قد يوقف الإضافة مؤقتا في الخلفية, ويفتح مربع حوار النظام عند الطلب; لا تطلب الإضافة ذلك من تلقاء نفسها, ولا تعتمد أي ميزة على هذا الاستثناء. قاست مصفوفة المراقبة في P5 فائدة هذا الاستثناء: بعد انطفاء الشاشة لفترة (Doze) يجمد Android شبكة تطبيقات الخلفية, فتفقد المراقبة اتصالها, وتنتهي مهلة إعادة اتصالها, ولا يبلغ عن البريد الجديد إلا بعد دقائق من استيقاظ الجهاز (نحو أربع دقائق على Android 9; وتعيد الإضافة الاتصال فورا عند انتهاء Doze); ومع الاستثناء تبقى المراقبة متصلة.
- تخدم أربعة أذونات المراقبة في الخلفية في الإصدار 1.1.0 ولا شيء غيرها. يشغل FOREGROUND_SERVICE و FOREGROUND_SERVICE_SPECIAL_USE خدمة المراقبة (النوع `specialUse`, النوع الفرعي `mail_background_watch`, لأن مراقبة البريد اتصال مفتوح ينتظر دفع الخادم وليست مزامنة بيانات محدودة) مع إشعار واحد منخفض الأولوية; يطلب POST_NOTIFICATIONS في صفحة المراقبات فقط عند تفعيل مراقبة ليمكن عرض الإشعار على Android 13 وما بعده; يدعم RECEIVE_BOOT_COMPLETED مفتاح التشغيل عند الإقلاع في تلك الصفحة, وهو معطل افتراضيا ولا يفعل المستقبل إلا عند تشغيله. لا تبدأ الخدمة إلا من صفحة المراقبات أو من اشتراك المضيف, ولا تتصل إلا بالحسابات المحفوظة, وتبقي الأسرار داخل عملية الإضافة, ويحمل بث الإيقاظ إلى AutoJs6 ظرف الرسالة (دون متن أبدا) ولا يصل إلا إلى مستقبل خلف إذن التوقيع PLUGIN.

احصل على المكون الإضافي فقط من صفحة [Releases](https://github.com/SuperMonster003/AutoJs6-Plugin-Angus-Mail/releases) الرسمية أو من مركز المكونات الإضافية في AutoJs6. قد تفشل الحزم من مصادر غير معروفة في التحقق من المضيف أو تحمل مخاطر حتى لو بدا رقم الإصدار متطابقا.

******

### واجهة المكون الإضافي

******

المعلومات التالية موجهة لمطوري مضيف AutoJs6 والمكونات الإضافية; يستخدم المضيف هذه المعرفات لاكتشاف المكون الإضافي والتفاوض على التوافق:

```text
application id: io.github.supermonster003.autojs6.plugin.angus.mail
plugin id: angus-mail
engine: mail
variant: default
service action: org.autojs.plugin.MAIL
service category: mail
info action: org.autojs.plugin.INFO
aidl interface: org.autojs.plugin.mail.api.IMailPlugin
minimum host build: 5282 (6.8.0)
```

تنفذ `AngusMailPluginService` عقد المضيف mail-api `org.autojs.plugin.mail.api.IMailPlugin` وتستجيب لـ `org.autojs.plugin.MAIL` (الفئة `mail`). تستجيب `AngusMailPluginInfoService` لـ `org.autojs.plugin.INFO` بكائن PluginInfo. تتيح `WakeActivity` للمضيف تنشيط المكون الإضافي.

******

### خارطة الطريق

******

تدار خطط المكون الإضافي وتقدمه كقائمة قابلة للتحقق في ROADMAP.md, منظمة حسب المرحلة مع معايير القبول ومستويات الأدلة. تعبر البنود غير المحددة عن النية لا عن القدرات الحالية; والنقاش عبر Issues موضع ترحيب.

- [عرض ROADMAP.md](https://github.com/SuperMonster003/AutoJs6-Plugin-Angus-Mail/blob/master/ROADMAP.md)

******

### سجل الإصدارات

******

#### v1.2.1

_2026/09/22_

- `إصلاح` حدث `closed` للمراقبة عند إغلاق الجلسة يحمل دائما السبب `closed`: كان خيط العمل في الجلسة قد يوقف بعض المراقبات أولا بالسبب `session-closed` (لوحظ مرة واحدة في مجموعة connected على محاكي API 24).

#### v1.2.0

_2026/09/22_

- `تلميح` لا يقدم تسجيل الدخول عبر المتصفح إلا في البناءات التي تحمل معرف عميل OAuth 2.0 للمزود (تسجيلات المشرف, تقرأ عند البناء من `oauth-clients.properties` الذي يتجاهله Git); البناء الذي لا يحملها يحتفظ بمساري الرمز وكلمة مرور التطبيق ويقول ذلك في مربع حوار المصادقة. يحتاج عميل Google إلى اجتياز التحقق من النطاقات الحساسة لمشروع Google Cloud قبل أن يعمل تسجيل الدخول لأي حساب; حتى ذلك الحين تقصره Google على مستخدمي الاختبار في المشروع.
- `ميزة` تسجيل الدخول عبر المتصفح لحسابات Google و Microsoft (خارطة طريق البريد P9): يقدم محرر الحساب "تسجيل الدخول بحساب Google (المتصفح)" للإعداد المسبق Gmail و "تسجيل الدخول بحساب Microsoft (المتصفح)" للإعدادين المسبقين Outlook.com و Microsoft 365; يفتح تسجيل الدخول صفحة المزود في Custom Tab (أي متصفح كبديل) بطلب رمز تفويض OAuth 2.0 يحمل PKCE (`S256`) و `state` عشوائيا, وتصل إعادة التوجيه (`<applicationId>://oauth2/microsoft`, أو مخطط معرف عميل Google المعكوس) إلى `OAuthRedirectActivity` التي تسلمها إلى شاشة تسجيل الدخول المنتظرة; ترفض الشاشة أي إعادة توجيه لا يطابق `state` فيها, وتستبدل الرمز عند نقطة الرموز عبر HTTPS (`HttpsFormPoster`, عميل HTTP الوحيد في الإضافة), وتملأ العنوان مسبقا من رمز الهوية
- `ميزة` تخزين الرموز وتجديدها: رموز تسجيل الدخول عبر المتصفح سجل مشفر واحد من النوع الجديد `OAUTH2` في مخزن الحسابات (ليس حقل Binder أبدا, ولا يظهر في `mail.accounts.list()` أبدا), ويحمل مستند الحساب كائن `oauth` (`provider`, `authorizedAt`, `expiresAt`, `needsReauth`) الذي تبلغ عنه `mail.accounts.list()`; تأخذ كل جلسة لهذا الاسم المستعار (السكربتات, اختبار الاتصال, المراقبة في الخلفية) رمز وصولها من `AccountSecrets` الذي يجدده عبر رمز التحديث عندما يتبقى أقل من خمس دقائق, بالتسلسل لكل حساب; التجديد المرفوض (`invalid_grant`) يعلم السجل `needsReauth`, وتفشل الجلسة بـ `AUTH_FAILED` ("sign in again"), وتعرض صفحة الحسابات "يلزم تسجيل الدخول مجددا" بجانب الحساب
- `ميزة` إجراءا صفحة الحسابات "تسجيل الدخول مجددا" (تسجيل دخول جديد عبر المتصفح يخزن في السجل نفسه) و "إلغاء تسجيل الدخول" (تستبدل رموز السجل فورا بعلامة إلغاء, ويطلب من Google إلغاء رمز التحديث, ويتوقف الحساب عن العمل حتى تسجيل دخول جديد); يذكر `authHint` للإعدادات المسبقة `gmail` و `outlook` و `office365` تسجيل الدخول عبر المتصفح أولا (`providers.json` الإصدار 4); 35 نصا جديدا في 11 لغة; اختبارات JVM لـ PKCE, وطلب التفويض وتحليل إعادة التوجيه, وعميل الرموز على نقل مكتوب, ومستند الرموز, وجدول المزودين, وعملاء البناء وعناوين إعادة التوجيه, و `AccountSecrets` (التجديد, وعلامة الرفض, والسجلات الملغاة, والمسح) وكائن `oauth` في خيارات الحساب والنموذج ومستند الحسابات

#### v1.1.0

_2026/09/21_

- `تلميح` المراقبة في الخلفية جديدة في 1.1.0 (خارطة طريق البريد P8): تبقي صفحة المراقبات في الإعدادات الحسابات المحفوظة تحت المراقبة في خدمة مقدمة بينما لا يعمل أي سكربت, وتوقظ مهمة AutoJs6 "عند وصول بريد". تحتاج هذه المهمة ومنتقي المراقبة إلى بنية المضيف التي تحمل إصدار عقد البريد 2 (AutoJs6 6.8.0 بعد البنية 5282); على مضيف أقدم تعرض الصفحة أنه لا يمكن إيقاظ AutoJs6 وتصل الرسائل الجديدة إلى قائمة سجلات المراقبة فقط. تضيف الميزة أربعة أذونات, يشرح كل منها قسم الأمان في README: FOREGROUND_SERVICE و FOREGROUND_SERVICE_SPECIAL_USE (خدمة المراقبة), POST_NOTIFICATIONS (إشعارها الدائم, يطلب فقط عند تفعيل مراقبة) و RECEIVE_BOOT_COMPLETED (مفتاح التشغيل عند الإقلاع في صفحة المراقبات, معطل افتراضيا).
- `ميزة` المراقبة في الخلفية (خارطة طريق البريد P8): اكتسبت الإعدادات صفحة مراقبات تضبط حتى 16 مراقبة (`MAX_TRIGGERS`) على الحسابات المحفوظة, لكل منها اسم واسم مستعار للحساب ومجلد ووضع (تلقائي أو IDLE أو استطلاع بفاصله) ومرشحات اختيارية للمرسل والموضوع, وتخزن في `mail-triggers/triggers.json` ضمن دليل بلا نسخ احتياطي; تشغل خدمة المقدمة `MailWatchService` من نوع `specialUse` المراقبات المفعلة على مراقبي P5 (IDLE حيث يدفع الخادم, واستطلاع في غير ذلك, وإعادة اتصال بتراجع, وإعادة اتصال فورية عند تغير الشبكة) بينما لا يعمل أي سكربت وتعرض إشعارا واحدا منخفض الأولوية; تضاف كل رسالة جديدة إلى قائمة سجلات المراقبة (آخر 100 ملخص ظرف, `MAX_TRIGGER_RECORDS`, دون متن أبدا) وتعرض الصفحة حالة الاتصال وآخر خطأ والسجلات وإجراء إعادة الاتصال; يفعل مفتاح التشغيل عند الإقلاع (معطل افتراضيا) مستقبل `BOOT_COMPLETED` الذي يعيد تشغيل الخدمة بعد إعادة التشغيل
- `ميزة` إصدار عقد البريد 2 (`IMailPlugin.openTrigger` / `listTriggers`, `IMailTrigger`, `IMailTriggerCallback`, ميزة القدرة `backgroundWatch`): يشترك المضيف في مراقبة مضبوطة بجيل ومرشح اختياري, ويتلقى الحالة الحالية فورا ثم `onStatus` (stopped أو connecting أو connected أو failed مع السبب وآخر خطأ) و `onMail(generation, seq, event)` لكل رسالة مطابقة; يحمل حدث `mail` معرف المراقبة والاسم المستعار والعنوان والمجلد وظرف الرسالة و `receivedAt`; تقبل المراقبة 4 مشتركين على الأكثر (`MAX_TRIGGER_SUBSCRIBERS`), وترفض المراقبة المعطلة أو المجهولة والخيارات غير الصالحة بحالة `stopped` سببها `refused`, ويستبدل `update` مرشح المشترك وينهي `stop` الاشتراك فقط; بدون مشترك حي يذهب كل حدث إلى AutoJs6 كبث صريح `org.autojs.autojs6.action.MAIL_TRIGGER` خلف إذن توقيعه `PLUGIN`, فيبدأ مهمة المضيف "عند وصول بريد" حتى عندما لا يعمل أي سكربت (`MailTriggerBinderTest` على AVD بواجهة API 37; `TriggerStoreTest`, `TriggerFilterTest`, `TriggerConfigTest`, `TriggerDocumentsTest`)
- `ميزة` اكتسب نواة البريد وثائق وقواعد التشغيل المشتركة بين الصفحة والخدمة و Binder (`TriggerConfig`, و `TriggerFilter` بسلاسل جزئية للمرسل والموضوع دون تمييز حالة الأحرف, `TriggerOptions`, `TriggerStatusDocument`, `TriggerEventDocument`, `TriggerRecord`) والحدود `MAX_TRIGGERS` و `MAX_TRIGGER_SUBSCRIBERS` و `MAX_TRIGGER_RECORDS` و `MIN_TRIGGER_INTERVAL_MS` (3 ثوان, خنق المضيف لكل مهمة), ويبلغ المراقبون `onConnected` لتعرض المراقبة في الخلفية `connected` بمجرد فتح مجلدها

##### لمزيد من سجل الإصدارات

* [CHANGELOG.md](https://github.com/SuperMonster003/AutoJs6-Plugin-Angus-Mail/blob/master/app/src/main/assets/doc/CHANGELOG-ar.md)

******

### البناء والتحقق

******

يستهدف هذا القسم المطورين الراغبين في بناء المكون الإضافي من المصدر; ويمكن للمستخدمين العاديين ببساطة تثبيت ملف APK الجاهز من صفحة Releases.

بناء APK للتصحيح:

```powershell
.\gradlew.bat :app:assembleDebug
```

تشغيل اختبارات وحدة JVM وبناء APK اختبارات الأجهزة:

```powershell
.\gradlew.bat :mail-core:test :app:testDebugUnitTest :app:assembleDebugAndroidTest
```

بناء APK الإصدار:

```powershell
.\gradlew.bat :app:assembleRelease
```

جمع ناتج الإصدار وإلحاق الإصدار وملخص CRC32 باسم الملف:

```powershell
.\gradlew.bat :app:appendDigestToReleasedFiles
```

التحقق من تزامن مصادر التوثيق متعدد اللغات مع النواتج المولدة (يفرض ذلك CI أيضا):

```powershell
py .python\generate_markdown.py --check
```

يتطلب البناء JDK 21 أو أحدث و Android SDK 37; وتدار إصدارات Gradle والمكونات الإضافية مركزيا عبر `version.properties` و `io.github.supermonster003.autojs6-platform-versions`.

******

### التعريب وتوليد التوثيق

******

```text
.readme/common.json
.readme/lang_*.json
.readme/template_readme.md
.readme/template_plugin_instruction.md
.changelog/lang_*.json
.changelog/template_changelog.md
.python/generate_markdown.py
app/src/main/assets/doc/CHANGELOG-*.md
app/src/main/res/raw-*/plugin_instruction.md
```

ملفات JSON اللغوية في `.readme/` و `.changelog/` هي المصدر الوحيد لملف README وتعليمات مركز المكونات الإضافية وسجل التغييرات. عدل دائما مصادر JSON هذه وأعد تشغيل `py .python/generate_markdown.py`; ولا تحرر يدويا نواتج README و `plugin_instruction.md` وسجل التغييرات المولدة أبدا. شغل `py .python/generate_markdown.py --check` للتحقق من جميع النواتج المولدة.

******

### الترخيص

******

كود المشروع مرخص بموجب [Mozilla Public License 2.0](https://github.com/SuperMonster003/AutoJs6-Plugin-Angus-Mail/blob/master/LICENSE). المكونات الخارجية وتراخيصها مدرجة في [إشعارات الجهات الخارجية](https://github.com/SuperMonster003/AutoJs6-Plugin-Angus-Mail/blob/master/THIRD_PARTY_NOTICES.md).

******

### روابط

******

- مشروع AutoJs6: https://github.com/SuperMonster003/AutoJs6
- توثيق AutoJs6: https://docs.autojs6.com
- توثيق وحدة البريد: https://docs.autojs6.com/#/mail
- Eclipse Angus Mail: https://eclipse-ee4j.github.io/angus-mail/
- إشعارات الجهات الخارجية: https://github.com/SuperMonster003/AutoJs6-Plugin-Angus-Mail/blob/master/THIRD_PARTY_NOTICES.md
